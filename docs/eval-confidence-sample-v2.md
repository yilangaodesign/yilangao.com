---
type: eval-spec
id: eval-confidence-sample-v2
topics: [eval, knowledge-graph, confidence]
references:
  - docs/eval-confidence-sample-v0.md
  - docs/eval-confidence-sample-v1.md
  - docs/eval-confidence-rubric.md
---

# Confidence Calibration — Labeling Sheet (with Context)

> **Version:** v2
>
> **Previous versions:**
> - [v0](eval-confidence-sample-v0.md) — raw technical data
> - [v1](eval-confidence-sample-v1.md) — plain language, no context
>
> **How to score:** Rate each connection from **0 to 10**. How useful would it be if an AI followed this link while working?
>
> | Score | What it means | Category |
> |-------|--------------|----------|
> | **0** | "I can't tell what this is for." | ambiguous |
> | **1–3** | "This would confuse or distract." | noise |
> | **4–6** | "Related, but I'd keep searching." | relevant |
> | **7–10** | "Exactly where I need to go." | authoritative |
>
> Just write a single number. Go with your gut.

---

## 🟢 High confidence (1.0) (20 connections)

The system is very confident these connections are real and useful.

---

### E001

**A report on graph health says it was created from the experiment plan.**

> **FROM:** `eval-graph-audit` — *Evaluation report*
> # Eval graph structural audit (Phase 0) > Read-only static analysis of `.cache/graph.json`. Generated by `scripts/audit-graph-structure.mjs`. No agent runs, no API costs. Re-runnable as a regression check. > > **Source cache**: `.cache/graph.json` > **Generated at**: 2026-05-03T03:04:49.984Z > **Schema version**: 1
>
> **TO:** `eval_experiment_program_0516e9af.plan` — *External/plan file*
> Plan file (not part of the docs knowledge graph — lives in .cursor/plans/)
>
> **How they're connected:** derivedFrom (detected via frontmatter)

**Score (0–10):** ___

---

### E002

**The engineering overview page mentions a critical recurring bug: agents reporting playground changes as done without flushing cache.**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `eap-042` — *Known mistake entry*
> ## EAP-042: Reporting Playground Changes as Done Without Flushing Cache and Restarting **Status: ACTIVE — ESCALATED (6+ violations)** **Trigger:** Editing a playground file (or any `src/` file consumed by the playground) and reporting the task as complete without flushing the Tu
>
> **How they're connected:** references (detected via citation:See)

**Score (0–10):** ___

---

### E003

**The AI instruction manual registers the case-study-writing skill so the AI knows it can use it.**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `skill-case-study-authoring` — *AI skill definition*
> # Skill: Case Study Authoring ## When to Activate **Phase 1a (Author from scratch):** - User provides raw materials: notes, transcripts, bullet points, project details, presentation slides, Cursor conversation logs. - User says "write a case study for X" or "turn these notes into a case study."
>
> **How they're connected:** triggers (detected via frontmatter)

**Score (0–10):** ___

---

### E004

**Bug report ENG-019 explicitly says "See EAP-013" — it hit this known mistake for the third time.**

> **FROM:** `eng-019` — *Bug/issue report*
> #### ENG-019: "Console Error — script tag warning persists with next/script; hydration mismatch in DesignSystemFootnote" **Issue:** Two console errors: 1. The React 19 script tag warning persisted even after ENG-018's fix (replacing `dangerouslySetInnerHTML` with `next/script` `
>
> **TO:** `eap-013` — *Known mistake entry*
> ## EAP-013: Script Tags in React 19 / Next.js 16 Component Trees **Status: ACTIVE** **Trigger:** Rendering a `<script>` element in any React component tree — via raw `<script>`, `dangerouslySetInnerHTML`, or `next/script` (`<Script>`).
>
> **How they're connected:** references (detected via citation:See)

**Score (0–10):** ___

---

### E005

**The engineering mistakes catalog physically contains EAP-077 as one of its entries in the Build/Toolchain/CSS category.**

> **FROM:** `engineering-anti-patterns` — *Known mistake entry*
> # Engineering Anti-Patterns > **What this file is:** A catalog of engineering mistakes that caused incidents or wasted time, documented so they are never repeated. Each anti-pattern includes the trigger, why it's wrong, and the correct alternative. > > **Who reads this:** AI agents before making code changes — scan for relevant anti-patterns. > **W
>
> **TO:** `eap-077` — *Known mistake entry*
> ## EAP-077: Assuming CSS Can Fix `<input>` Native Text Clipping for Oversized Serif Fonts **Trigger:** A serif font at display scale (2rem+) is used inside a native `<input>` element. Glyphs with negative left side-bearing (serif hooks, tittles) appear clipped at the leftmost pi
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E006

**The About Page sub-topic declares in its header that it belongs to the Content hub — like a chapter saying which book it's in.**

> **FROM:** `content-about-page` — *Sub-topic page*
> # §9. About Page & Supporting Content ### 9.1 Purpose The About page is a secondary conversion asset. Hiring managers visit it when already interested. It should **reinforce**, not introduce, the value proposition. ### 9.2 Useful Elements - A brief professional narrative (how you got here, what you care about) — 2-3 paragraphs max - Specific skills
>
> **TO:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **How they're connected:** derivedFrom (detected via frontmatter)

**Score (0–10):** ___

---

### E007

**The engineering mistakes catalog physically contains EAP-110 as one of its entries.**

> **FROM:** `engineering-anti-patterns` — *Known mistake entry*
> # Engineering Anti-Patterns > **What this file is:** A catalog of engineering mistakes that caused incidents or wasted time, documented so they are never repeated. Each anti-pattern includes the trigger, why it's wrong, and the correct alternative. > > **Who reads this:** AI agents before making code changes — scan for relevant anti-patterns. > **W
>
> **TO:** `eap-110` — *Known mistake entry*
> ## EAP-110: Reorder-Only DnD on a 2-D Layout **Status: ACTIVE** **Trigger:** A drag-and-drop system is wired up on a 2-D layout (grid, row/column composition, masonry, anything where the dragged item can be adjacent to the drop target on two different axes) and the DnD handlers
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E008

**The AI rulebook physically contains design rule #1: "NEVER use inline style={{}} — always use Tailwind classes."**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `guardrail-design-1` — *AI rule (do/don't)*
> 1. **NEVER** use inline `style={{}}` — always use Tailwind classes or CSS custom properties 2. **NEVER** place CSS resets outside `@layer base` — unlayered resets override all Tailwind utilities 3. **NEVER**
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E009

**The AI instruction manual registers the password-gate skill.**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `skill-password-gate` — *AI skill definition*
> # Password Gate — Architecture Reference > **Read this before modifying:** `src/proxy.ts`, `src/collections/Companies.ts`, > `src/lib/company-session.ts`, `src/lib/company-data.ts`, or any file under `src/app/(frontend)/for/`. ## System Overview The main site is behind a server-side password gate. All unauthenticated requests are intercepted by `sr
>
> **How they're connected:** triggers (detected via frontmatter)

**Score (0–10):** ___

---

### E010

**The engineering mistakes catalog physically contains EAP-068 in its Dev Workflow category.**

> **FROM:** `engineering-anti-patterns` — *Known mistake entry*
> # Engineering Anti-Patterns > **What this file is:** A catalog of engineering mistakes that caused incidents or wasted time, documented so they are never repeated. Each anti-pattern includes the trigger, why it's wrong, and the correct alternative. > > **Who reads this:** AI agents before making code changes — scan for relevant anti-patterns. > **W
>
> **TO:** `eap-068` — *Known mistake entry*
> ## EAP-068: Treating TCP Listen as a Health Check for Dev Servers **Status: ACTIVE** **Trigger:** Using `lsof`, `nc`, or `ss` to confirm a port is occupied and concluding the dev server is healthy.
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E011

**The engineering overview mentions a critical escalated problem: playground and production versions keep drifting apart (EAP-007).**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `eap-007` — *Known mistake entry*
> ## EAP-007: Adding Components to Main Site Without Playground Preview **Status: ACTIVE** **Trigger:** Creating a new reusable component in `src/components/` and integrating it into pages, but not creating a corresponding preview page in the playground (`playground/src/app/compon
>
> **How they're connected:** references (detected via citation:See)

**Score (0–10):** ___

---

### E012

**The AI instruction manual registers the content stress-test skill.**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `skill-stress-test` — *AI skill definition*
> # Skill: Retroactive Content Stress Test Re-evaluates all active case studies through the lens of the current knowledge base. Runs every check and anti-pattern test, auto-fixes only deterministic issues from a strict allowlist, and reports everything else for user decision. ## When to Activate - User says "content stress test", "fresh eyes", "reche
>
> **How they're connected:** triggers (detected via frontmatter)

**Score (0–10):** ___

---

### E013

**The AI rulebook physically contains engineering rule #15: "NEVER use barrel imports from lucide-react in the playground."**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `guardrail-engineering-15` — *AI rule (do/don't)*
> 15. **NEVER** use barrel imports from `lucide-react` in the playground (`import { X } from "lucide-react"`) — Turbopack's `optimizePackageImports` resolves named exports to wrong icon components on server vs client, causing hydration mismatches. Use individual im
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E014

**The release history archive contains release #14 as one of its entries.**

> **FROM:** `release-log-archive` — *Historical release notes*
> # Release Log — Archive > **What this file is:** Archived release log entries that exceeded the 15-entry cap in `docs/release-log.md`. Kept for historical reference. > > **Last updated:** 2026-04-30 (archived REL-001 through REL-021) ---
>
> **TO:** `rel-014` — *Single release entry*
> ## REL-014 — Élan 2.11.2, yilangao.com 1.3.2, ASCII Art Studio 0.6.4 (2026-04-22) **Scope:** 3 files across 2 dependency-ordered layer commits (L1 docs, L8 frontend). yilangao.com: essay layout token change per FB-170. Élan and ASCII Art Studio: manifest sync releases.
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E015

**The stress-test skill says it operates on content — its description mentions "content stress test."**

> **FROM:** `skill-stress-test` — *AI skill definition*
> # Skill: Retroactive Content Stress Test Re-evaluates all active case studies through the lens of the current knowledge base. Runs every check and anti-pattern test, auto-fixes only deterministic issues from a strict allowlist, and reports everything else for user decision. ## When to Activate - User says "content stress test", "fresh eyes", "reche
>
> **TO:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **How they're connected:** references (detected via frontmatter)

**Score (0–10):** ___

---

### E016

**The content mistakes catalog physically contains CAP-005 in its Information Architecture category.**

> **FROM:** `content-anti-patterns` — *Known mistake entry*
> # Content Anti-Patterns > **What this file is:** A catalog of content mistakes that weaken portfolio effectiveness, documented so they are never repeated. Each anti-pattern includes the trigger, why it's wrong, and the correct alternative. > > **Who reads this:** AI agents before writing or editing portfolio content — scan for relevant anti-pattern
>
> **TO:** `cap-005` — *Known mistake entry*
> ## CAP-005: Overloaded Project Count **Status: ACTIVE** **Trigger:** Featuring 8-15 projects on the homepage, including uneven quality or projects from significantly different career stages.
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E017

**The engineering mistakes catalog declares that it enforces standards on the Engineering hub — a governance relationship.**

> **FROM:** `engineering-anti-patterns` — *Known mistake entry*
> # Engineering Anti-Patterns > **What this file is:** A catalog of engineering mistakes that caused incidents or wasted time, documented so they are never repeated. Each anti-pattern includes the trigger, why it's wrong, and the correct alternative. > > **Who reads this:** AI agents before making code changes — scan for relevant anti-patterns. > **W
>
> **TO:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **How they're connected:** enforces (detected via frontmatter)

**Score (0–10):** ___

---

### E018

**The AI rulebook physically contains design rule #7: "NEVER use SVG to render text, labels, or component UI."**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `guardrail-design-7` — *AI rule (do/don't)*
> 7. **NEVER** use SVG to render text, labels, or component UI — SVG is permitted only for icons, logos, and decorative illustrations. Text in SVG bypasses the typography token system, breaks copy/paste, and is invisible to screen readers. 8. <
>
> **How they're connected:** documents (detected via anchor:contained)

**Score (0–10):** ___

---

### E019

**The AI instruction manual registers the documentation audit skill.**

> **FROM:** `AGENTS` — *AI instruction manual*
> graph metadata for docs knowledge graph (see docs/knowledge-graph.md) --> # This is NOT the Next.js you know This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices. # Orchestrator Ove
>
> **TO:** `skill-doc-audit` — *AI skill definition*
> # Skill: Doc Self-Audit Periodic health check for the agent knowledge base. Combines static analysis with AI-powered semantic review to keep docs organized, deduplicated, and discoverable. ## When to Activate - User says "audit my docs", "check doc health", or similar - AGENTS.md pre-flight suggests it (7+ days since last audit) - After a session t
>
> **How they're connected:** triggers (detected via frontmatter)

**Score (0–10):** ___

---

### E020

**Bug report ENG-021 explicitly says "Anti-pattern: See EAP-054."**

> **FROM:** `eng-021` — *Bug/issue report*
> #### ENG-021: "Admin view doesn't reflect the website structure" **Issue:** The user opened the Payload admin panel and saw only 4 collections (Users, Media, Projects, Books) + 1 global (SiteConfig), but the frontend website has 10+ pages. Several pages (Contact, Experiments) ha
>
> **TO:** `eap-054` — *Known mistake entry*
> ## EAP-054: Client Mutation Without router.refresh() in App Router **Status: ACTIVE** **Trigger:** A client component POSTs/PATCHes data to the backend (e.g., CMS global, collection item) and then updates local React state (e.g., exits a modal, clears a form) — but does **not**
>
> **How they're connected:** references (detected via citation:See)

**Score (0–10):** ___

---

## 🟡 Medium confidence (0.6) (20 connections)

Moderately confident — detected from markdown links or "Related:" mentions rather than explicit "See:" citations.

---

### E021

**Bug report ENG-200 says a principle was "extracted" into EAP-118 (session check without identity match). Phrased as "Related:" not "See:".**

> **FROM:** `eng-200` — *Bug/issue report*
> ### ENG-200: Frozen-Point TypeError from link popover focus stealing Lexical selection **Issue:** Console TypeError `Cannot assign to read only property 'key' of object '#<Point>'` appearing after the ENG-198/199 link popover and ClickableLinkPlugin changes. **Root Cause:** Two
>
> **TO:** `eap-118` — *Known mistake entry*
> ## EAP-118: Native `Selection` moves across Lexical editors without blurring the outgoing surface **Status: ACTIVE** **Trigger:** A page mounts **multiple** `LexicalComposer` instances (one per rich-text block). A keyboard bridge (`BlockNavPlugin`, custom command, or structural
>
> **How they're connected:** references (detected via citation:Related:)

**Score (0–10):** ___

---

### E022

**A repeated section of feedback entry #104 contains a markdown link to entry #230. The link is in a "Last updated" header note, not the entry body.**

> **FROM:** `eng-104-occ2` — *Section within a larger document*
> ### ENG-104: "Local preview and deployed looks different - paragraph separation and bolding lost" **Date:** 2026-04-08
>
> **TO:** `eng-230` — *Bug/issue report*
> ### ENG-230: Plan B remediation — adversarial audit closed 10 quality gaps **Date:** 2026-04-26 **Issue:** Self-administered adversarial audit on Plan B (Feedback Log AP-Tagging) surfaced 10 distinct quality gaps spanning enforcement softness, methodological selection bias, plan
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E023

**The Design hub has a table-of-contents link to its Layout sub-page — a navigational index link.**

> **FROM:** `design` — *Hub page (main topic overview)*
> # Design System — Accumulated Knowledge > **What this file is:** The hub file for the design knowledge base. Detailed section content lives in spoke files under `docs/design/`. This file contains the Section Index for routing, the Design Posture (read every time), Process Principles, and the Feedback Frequency Map. > > **Who reads this:** AI agents
>
> **TO:** `design-layout` — *Sub-topic page*
> # Layout Integrity > Spoke file for `docs/design.md`. Return to the [hub](design.md) for the Section Index. ### 2.1 No Overlapping Navigation and content must coexist as **flex siblings**, never overlap via z-index. The pattern: ``` <div class="flex min-h-screen">
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E024

**An anti-pattern entry's file has an HTML comment referencing the knowledge graph spec. The link is infrastructure metadata, not topical content.**

> **FROM:** `eap-120` — *Known mistake entry*
> - **`subpath` resolves against markdown headings, NOT HTML `<a id>` anchors.** Our docs use the pattern `\n## EAP-120: ...` — the HTML anchor is what `.cache/graph.json` records, but Obsidian's `subpath` field looks for a heading literally named `eap-120` and fails. The exporter
>
> **TO:** `knowledge-graph` — *Specification document*
> # Docs Knowledge Graph - Canonical Spec > **Single source of truth** for the knowledge graph layered over the documentation system. Every other document in the system - hubs, spokes, anti-patterns, skills, rules, feedback logs, release logs, the AGENTS.md route table - participates in this graph by following the conventions defined here. > > **Auth
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E025

**Design feedback #178 says "Related: AP-065" — flagged as a loose/approximate match.**

> **FROM:** `fb-178` — *Bug/issue report*
> ### FB-178: CollaborationLoop overuses brand accent — neutralize decorative color **Date:** 2026-04-23 **Issue:** User flagged that the CollaborationLoop component (Elan case study) abuses the brand accent color (Lumen blue-violet). Nearly every structural element - step dots, tr
>
> **TO:** `ap-065` — *Known mistake entry*
> ## AP-065: Centered-Align Footer Text Below Left-Aligned Form Elements **Trigger:** Using `text-align: center` on helper/footer text below a left-aligned input field or form group, where the form and footer share a narrow container. **Why it's wrong:** The footer's visual left ed
>
> **How they're connected:** references (detected via citation:Related:)

**Score (0–10):** ___

---

### E026

**The Content hub has a table-of-contents link to its Conversion Funnel sub-page.**

> **FROM:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **TO:** `content-conversion-funnel` — *Sub-topic page*
> # §1. Conversion Funnel & Hiring Manager Mental Model **Source:** `portfolio-hiring-manager-mental-model.md` (v2, RARRA + lifecycle model) + competitive analysis, 2026-03-29. ## 1.1 The Funnel Every portfolio visit is a five-stage conversion funnel. Each stage has a job, a signal the hiring manager seeks, and a failure mode. | Stage | Time | Signal
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E027

**A section about auto-created node types links to the cleanup decisions doc for context on authority ordering.**

> **FROM:** `kg-synthetic-types` — *Section within a larger document*
> ### 8.1 Build-derived synthetic node types The build script (`scripts/build-graph.mjs`) auto-generates nodes of the following types from anchor patterns. These types have no source frontmatter — they are build artifacts. They are recognized by the schema check but sho
>
> **TO:** `knowledge-graph-hygiene-decisions` — *Specification document*
> # Knowledge Graph Hygiene Decisions > **Purpose:** Record the decisions made during Plan D (Graph Hygiene Remediation) so future agents and contributors understand *why* the graph is shaped the way it is, not just *what* it looks like. > > **Status:** LOCKED. D3 and D4 may proceed. >
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E028

**The Content hub has a table-of-contents link to its Analytics & Measurement sub-page.**

> **FROM:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **TO:** `content-analytics-measurement` — *Sub-topic page*
> # Analytics Measurement Strategy > **What this file is:** Content spoke for interpreting client analytics data and deciding what to measure next. Maps Mixpanel events to portfolio optimization questions. > > **Who reads this:** Agents routed here by `AGENTS.md` Pre-Flight route 17 → `docs/analytics.md` → this file. > > **Last updated:** 2026-04-25 
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E029

**The Content hub has a table-of-contents link to its Seniority Signals sub-page.**

> **FROM:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **TO:** `content-seniority-signals` — *Sub-topic page*
> # §6. Seniority Signals ### 6.1 How Hiring Managers Calibrate Level Seniority is not read from a title on the page. It's read from **how the designer talks about their work.** The differentiators: | Junior Signal | Senior Signal | |---|---| | "I was asked to redesign the dashboard" | "I identified that dashboard engagement was dropping and proposed
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E030

**An engineering sub-page about analytics tracking links to the shared analytics reference doc.**

> **FROM:** `engineering-analytics-instrumentation` — *Sub-topic page*
> # §16. Analytics Instrumentation > **What this file is:** Engineering spoke for client-side analytics (Mixpanel + Vercel Analytics). Covers the provider architecture, hook internals, naming conventions, and how to add or modify events. > > **Who reads this:** Agents routed here by `AGENTS.md` Pre-Flight route 17 → `docs/analytics.md` → this file. >
>
> **TO:** `analytics` — *Shared reference (used across multiple pillars)*
> # Client Analytics Reference > **What this file is:** A cross-cutting reference consumed by both engineering and content work streams. It is **not a hub** — it has no spokes of its own. Implementation details live in [`docs/engineering/analytics-instrumentation.md`](engineering/analytics-instrumentation.md); measurement strategy lives in [`docs/con
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E031

**Bug report ENG-197 says "Related: EAP-119" — a loose/heuristic match.**

> **FROM:** `eng-197` — *Bug/issue report*
> ### ENG-197: Lexical editor crashes on CMS data with Payload-format links **Issue:** Console TypeError "Cannot read properties of undefined (reading 'match')" thrown from `LexicalBlockEditor.useState` during editor initialization. The error appeared whenever a richText block con
>
> **TO:** `eap-119` — *Known mistake entry*
> ## EAP-119: Using `@lexical/link` `LinkNode` for Content Rendered by Payload's `convertLexicalToHTML` **Status: ACTIVE** **Trigger:** `markdownToLexical()` (or any headless Lexical editor that registers `@lexical/link`'s `LinkNode`) produces serialized JSON with link nodes shape
>
> **How they're connected:** references (detected via citation:Related:)

**Score (0–10):** ___

---

### E032

**Content feedback #13 says "Related: CAP-003" — a loose/heuristic match.**

> **FROM:** `cfb-013` — *Bug/issue report*
> #### CFB-013: "Spacing and radius is irrelevant — section is about color, token naming was missing" **Intent:** The "Lumen — A Custom Color Identity" section in the Élan case study should exclusively showcase color-related content. The interactive TokenGrid had Spacing and Radiu
>
> **TO:** `cap-003` — *Known mistake entry*
> ## CAP-003: Screenshot Gallery Without Narrative Context **Status: ACTIVE** **Trigger:** A page full of screenshots with no text explaining what the reader is looking at, why it matters, or what decisions it represents.
>
> **How they're connected:** references (detected via citation:Related:)

**Score (0–10):** ___

---

### E033

**The Engineering hub has a table-of-contents link to its Debugging sub-page.**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `engineering-debugging` — *Sub-topic page*
> # Debugging Methodology > Spoke file for `docs/engineering.md`. Return to the [hub](engineering.md) for the Section Index. ## 4.1 Diagnose Before You Patch When something "doesn't work," the root cause is usually one of: 1. **Data sync issue** — the UI reads from a different source than what was modified. 2. **Process issue** — the server isn't 
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E034

**The Engineering hub has a table-of-contents link to its Versioning sub-page.**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `engineering-versioning` — *Sub-topic page*
> # Multi-App Versioning > Spoke file for `docs/engineering.md`. Return to the [hub](engineering.md) for the Section Index. **Source:** Session 2026-03-29 — "Let's start doing version control for the design system." Extended 2026-04-09 to cover the main website. ## 10.1 Overview Every app in the monorepo with its own deployment lifecycle has a **v
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E035

**The Storage sub-page says "for external video embeds, see media-embeds.md" — a cross-reference between sibling topics.**

> **FROM:** `engineering-storage` — *Sub-topic page*
> # Media & File Storage (Supabase Storage) > Spoke file for `docs/engineering.md`. Return to the [hub](engineering.md) for the Section Index. **Source:** Session 2026-03-30, ENG-053 — "How do I store assets? Where are thumbnails and resumes stored?" **Updated:** 2026-04-20 — Added scope-boundary cross-reference to media-embeds.md (ENG-153) > **Sc
>
> **TO:** `engineering-media-embeds` — *Sub-topic page*
> # External Video Embeds (YouTube / Vimeo / Loom) > Spoke file for `docs/engineering.md`. Return to the [hub](engineering.md) for the Section Index. **Source:** Session 2026-04-20, ENG-153 — "Add external video embed support to case studies (Path A: new `videoEmbed` block)." **Updated:** 2026-04-20 — Initial creation. > **Scope: external embed re
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E036

**The knowledge graph spec's table of contents links to its own section 14 (Cluster Validation). An internal nav link within the same file.**

> **FROM:** `kg-section-index` — *Section within a larger document*
> ## Section index - [1. Goals and non-goals](#kg-goals) - [2. Anchor convention](#kg-anchors)
>
> **TO:** `kg-clusters` — *Section within a larger document*
> ## 14. Cluster validation `scripts/cluster-check.mjs` runs Louvain community detection ([`graphology-communities-louvain`](https://www.npmjs.com/package/graphology-communities-louvain)) on the built graph, compares detected communities against pillar frontmatter (`hub-design
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E037

**The knowledge graph spec's table of contents links to its own section 16 (Maintenance Rules). Same internal nav pattern.**

> **FROM:** `kg-section-index` — *Section within a larger document*
> ## Section index - [1. Goals and non-goals](#kg-goals) - [2. Anchor convention](#kg-anchors)
>
> **TO:** `kg-maintenance` — *Section within a larger document*
> ## 16. Maintenance rules **When to rebuild the graph.** After ANY structural change to docs (new heading, renamed anchor, new edge in frontmatter, new file). Engineering Hard Guardrail 27 in AGENTS.md (added in Phase 7b) codifies this: `npm run build-graph && npm run audi
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E038

**The Content hub has a table-of-contents link to its Portfolio Coherence sub-page.**

> **FROM:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **TO:** `content-portfolio-coherence` — *Sub-topic page*
> # §18. Portfolio Coherence Manifest > **What this file is:** A living inventory tracking stylistic and structural diversity across all active case studies. Ensures the portfolio doesn't repeat the same narrative shape, headline technique, or technique emphasis across multiple entries. > > **Who reads this:** AI agents during case study authoring (d
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E039

**The Media Embeds sub-page links back to Storage — the reverse of E035. Two sibling pages pointing at each other.**

> **FROM:** `engineering-media-embeds` — *Sub-topic page*
> # External Video Embeds (YouTube / Vimeo / Loom) > Spoke file for `docs/engineering.md`. Return to the [hub](engineering.md) for the Section Index. **Source:** Session 2026-04-20, ENG-153 — "Add external video embed support to case studies (Path A: new `videoEmbed` block)." **Updated:** 2026-04-20 — Initial creation. > **Scope: external embed re
>
> **TO:** `engineering-storage` — *Sub-topic page*
> # Media & File Storage (Supabase Storage) > Spoke file for `docs/engineering.md`. Return to the [hub](engineering.md) for the Section Index. **Source:** Session 2026-03-30, ENG-053 — "How do I store assets? Where are thumbnails and resumes stored?" **Updated:** 2026-04-20 — Added scope-boundary cross-reference to media-embeds.md (ENG-153) > **Sc
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

### E040

**The knowledge graph spec's table of contents links to its own section 3 (ID Naming). Same internal nav pattern.**

> **FROM:** `kg-section-index` — *Section within a larger document*
> ## Section index - [1. Goals and non-goals](#kg-goals) - [2. Anchor convention](#kg-anchors)
>
> **TO:** `kg-id-naming` — *Section within a larger document*
> ## 3. ID naming | Heading shape | Anchor ID | Example | |---|---|---|
>
> **How they're connected:** references (detected via markdown-link)

**Score (0–10):** ___

---

## 🔴 Low confidence (0.5) (20 connections)

Low confidence — detected from bare ID mentions. The system spotted a code or ID in the text but isn't sure it's a deliberate cross-reference.

---

### E041

**The engineering overview's summary table mentions EAP-115 (verification gap) in a row — it's listed but not a deliberate "See" citation.**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `eap-115` — *Known mistake entry*
> ## EAP-115: Fix Verification Deferred to "Next Live Session" Under Webpack HMR Staleness **Status: ACTIVE** **Trigger:** A fix touches structural React patterns inside a client component — hook shape changes (`useMemo` → `useState`, `useState` → `useRef`), added/removed refs, re
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E042

**A section of the AI instructions mentions FB-040 as an example in a paragraph about multi-category feedback logging.**

> **FROM:** `route-knowledge-d` — *Section of the AI instruction manual*
> D. **Are you touching graph metadata (frontmatter, anchors, edges)?** → Read `docs/knowledge-graph.md` Section Index, then the relevant subsection. → After edits, run `npm run audit-docs` to validate topology. # Mid-Flight: Verification Gate
>
> **TO:** `fb-040` — *Bug/issue report*
> #### FB-040: "It's really unclear about what the second field is" **UX Intent:** When editing structured data in a panel (like a Teams list with Company, Period, and Website fields), every input must communicate what it is and what format it expects. The user should never have to
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E043

**Feedback entry #253 mentions #252 because they're adjacent entries in the log — physical proximity, likely not a deliberate cross-reference.**

> **FROM:** `eng-253` — *Bug/issue report*
> ### ENG-253: Canvas label still rendering on hover despite tooltip — own paintNode was the source **Issue:** White text label still appeared below hovered nodes even after adding `nodeCanvasObjectMode="replace"` and suppressing the library's tooltip. The previous fix (ENG-252) a
>
> **TO:** `eng-252` — *Bug/issue report*
> ### ENG-252: Ghost node label still visible despite nodeLabel suppression **Issue:** A faint text label appeared near the hub node on the canvas, despite `nodeLabel={() => ""}` and CSS `.float-tooltip-kap { display: none }` being set. **Root cause:** `nodeCanvasObjectMode` was n
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E044

**A housekeeping doc tracking ID renumbering lists ENG-202 in a table of changed line numbers. A bookkeeping reference, not topical.**

> **FROM:** `eng-renumber-log` — *ID renumbering tracker*
> > **Last updated:** 2026-04-26 (Plan B Phase 4a.2.(i) — duplicate-anchor disambiguation + ENG-219 fresh allocation) # Engineering ID Renumber & Anchor Log > Tracks ID retrofits, fresh ENG-NNN allocations, and duplicate-anchor disambiguation in `docs/engineering-feedback-log.md` and `docs/engineering-feedback-log-archive.md`. ## Scope Plan B Phase 4
>
> **TO:** `eng-202` — *Bug/issue report*
> ### ENG-202: Company-personalized badge on home page + essay badge cleanup **Issue:** User wanted a lightweight personalization signal on the home page - a badge above case study titles that appears only when a company visitor has logged in through the password gate and the comp
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E045

**The Voice & Style doc mentions CAP-023 (voice flattening during refinement) in a list of related anti-patterns — topically relevant to voice.**

> **FROM:** `content-voice-style` — *Sub-topic page*
> # Section 13 — Voice & Style > Two-tier scoping: **universal rules** apply to all case study text work > (iteration and authoring). **Authoring-mode rules** apply only to new case > study creation or full rebuilds. ## 13.1 Universal Rules These rules apply whenever an agent writes or edits any case study text —
>
> **TO:** `cap-023` — *Known mistake entry*
> ## CAP-023: Voice Flattening During Refinement **Status: ACTIVE** **Trigger:** Taking a user-provided raw draft that is vivid, specific, and personal, and "refining" it into text that is grammatically correct but stripped of the original's energy - fragment rhythm replaced with
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E046

**Engineering bug #139 mentions design feedback #129 — both document the same CSS overflow/serif-font incident from different angles.**

> **FROM:** `eng-139` — *Bug/issue report*
> ### ENG-139: Browser `<input>` native text clipping unfixable via CSS **Date:** 2026-04-08 **Issue:** IBM Plex Serif lowercase "j" at 2.75rem had its top-left tittle/serif clipped when rendered inside a native `<input>` element on the login page. The glyph extends left of its or
>
> **TO:** `fb-129` — *Bug/issue report*
> ### FB-129: Login page — serif glyph clipping on `<input>` elements **Date:** 2026-04-08 **User direction:** The lowercase "j" in IBM Plex Serif at 2.75rem had its top-left tittle dot clipped when rendered inside a native `<input>` element. Multiple attempts at CSS fixes (padding
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E047

**Content anti-pattern CAP-032 lists ENG-233 in a "Reference:" footnote alongside other IDs. A citation cluster connecting content and engineering issues.**

> **FROM:** `cap-032` — *Known mistake entry*
> ## CAP-032: Redundant Noun in Labelled Count **Status: ACTIVE** **Category:** UX Microcopy
>
> **TO:** `eng-233` — *Bug/issue report*
> ### ENG-233: Systematic fix for missing paragraph spacing in richText prose containers **Date:** 2026-04-26 **Issue:** After ENG-202 patched `.introBlurbBody` with an inline `p + p` rule, the same bug remained latent on `.sectionBody` (case study section bodies) and `.sliderText
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E048

**The Elan Design System case study mentions content feedback #45 in parentheses as supporting evidence.**

> **FROM:** `content-elan-design-system` — *Case study page*
> # Élan Design System (renamed to Engram) > **Renamed:** This case study is now **Engram**. Canonical file: `engram.md` (id: `content-engram`). > Slug changed from `elan-design-system` to `engram` on 2026-04-29. > This file is retained as a graph alias so that references to `content-elan-design-system` > resolve to the canonical node via the `refere
>
> **TO:** `cfb-045` — *Bug/issue report*
> #### CFB-045: "The tag here needs to be improved. What is this case study really about?" **Intent:** The sidebar category tag is the fastest signal an interviewer reads. "Design Systems · Self-Improving AI Collaboration" buries the lead — "Design Systems" is generic (every UX de
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E049

**The engineering overview's CMS issue table lists ENG-123 among 54 related incidents. The ID is one of many in a long sequence.**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `eng-123` — *Bug/issue report*
> ### ENG-123: Hero image upload — uniqueness error + display disconnect **Date:** 2026-04-06 **Issue:** Two problems with hero image upload on case study pages: (1) Uploading images via the hero ImageUploadZone fails with "Could not save - A field value must be unique" when the s
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E050

**ENG-020 reflects: "This is the same escalation pattern as ENG-012 (documentation skips)." A deliberate parallel drawn in a meta-lesson.**

> **FROM:** `eng-020` — *Bug/issue report*
> #### ENG-020: "Why are there so many constant-like errors that I have to tell you?" **Issue:** The user had to manually copy-paste three consecutive console errors (ENG-017 script tag, ENG-018 script tag again, ENG-019 script tag + hydration mismatch) because the agent reported
>
> **TO:** `eng-012` — *Bug/issue report*
> #### ENG-012: "You need to document things. Why are you not following it? Check the root cause and fix it." **Issue:** User identified that ENG-009, ENG-010, and ENG-011 were all fixed without any documentation — no engineering feedback log entries, no anti-pattern updates, no f
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E051

**The engineering overview's hydration mismatch table lists ENG-087 as one of 10+ related incidents in a sequence.**

> **FROM:** `engineering` — *Hub page (main topic overview)*
> # Engineering — Accumulated Knowledge > **What this file is:** The hub of the engineering knowledge base for yilangao.com. Detailed topic sections live in `docs/engineering/*.md` spoke files; this hub contains the Section Index, meta-principles, and the incident frequency map. Every principle here was extracted from real incidents, debugging sessio
>
> **TO:** `eng-087` — *Bug/issue report*
> #### ENG-087: "Hydration failed — sidebar icon SVG differs between server and client (lucide-react barrel import)" **Issue:** Console hydration error on every playground page: "Hydration failed because the server rendered HTML didn't match the client." The diff showed the server
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E052

**ENG-031 traces its history: "CMS UX / inline editing — fifth occurrence (ENG-027→031). Escalation trigger activated." A deliberate chain reference.**

> **FROM:** `eng-031` — *Bug/issue report*
> #### ENG-031: "I don't think the actual configuration dashboard is reflecting the additions" **Issue:** The user reported that fields added to the frontend inline editing UI (like the `period` field on Teams) were not reflected in the Payload admin dashboard. The user identified
>
> **TO:** `eng-027` — *Bug/issue report*
> #### ENG-027: Inline CMS editing — Figma-like hover/edit/save on frontend **Issue:** Content editing required navigating to the Payload admin panel — no way to edit text directly on the live site. **Resolution:** Built a full inline editing system with these components:
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E053

**The Content hub's issue table mentions CAP-025 (floating metric without derivation) in a summary row.**

> **FROM:** `content` — *Hub page (main topic overview)*
> # Content Strategy — Accumulated Knowledge > **What this file is:** The hub file for the content strategy knowledge base. Detailed section content lives in spoke files under `docs/content/`. This file contains the Section Index for routing, the Content Posture (read every time), and the Feedback Frequency Map. > > **Who reads this:** AI agents rout
>
> **TO:** `cap-025` — *Known mistake entry*
> ## CAP-025: Floating Metric **Status: ACTIVE** **Trigger:** A hero metric that appears in the visual scan zone (blurb headline, hero area, or scope statement) without a derivation or anchor visible within the scope statement or blurb body closer. The metric type determines what
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E054

**Engineering entry #187 mentions design feedback #163 in a "Last updated" header note — likely housekeeping, not a topical cross-reference.**

> **FROM:** `eng-187` — *Bug/issue report*
> ### ENG-187: "Why tf are both states thicker and thicker?? Default should be 1px, thick active 2px." **Date:** 2026-04-21 **Issue:** Fourth complaint in the same session on the DS `Input` border thickness. User provided two screenshots: the engaged state reads as a very thick so
>
> **TO:** `fb-163` — *Bug/issue report*
> ### FB-163: "Why tf are both states thicker and thicker?? Default should be 1px, thick active 2px." **Date:** 2026-04-21 **Issue:** Fourth complaint on DS `Input` border thickness in a single session. User: "Even the default is 2px" — correct. The shipped ENG-186 architecture ren
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E055

**Engineering entry #170 mentions design feedback #157 in a "Last updated" header note — same housekeeping pattern as E054.**

> **FROM:** `eng-170` — *Bug/issue report*
> ### ENG-170: Split `media.muted` into capability + default-state; remove audio control from `videoEmbed` **Date:** 2026-04-20 **Issue / Capability:** ENG-169 (same session) shipped an editor-facing "muted by default" ButtonSelect on both `VideoSettings.tsx` (uploaded videos) and
>
> **TO:** `fb-157` — *Bug/issue report*
> ### FB-157: "You're conflating two different things, and that's very dangerous" — audio capability vs. default state on video settings **Date:** 2026-04-20 **UX Intent:** Yesterday's FB-154 / ENG-169 promoted a single "Muted by default / Sound by default" `ButtonSelect` onto `Vid
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E056

**Engineering entry #102 mentions EAP-007 (playground/production drift) in passing during a CMS parity check note.**

> **FROM:** `eng-102` — *Bug/issue report*
> ### ENG-102: "I don't see the label component being there in the playground shell" **Date:** 2026-04-03 **Issue:** Created the `Label` component (`src/components/ui/Label/`) with full implementation (6 sizes, 2 emphasis levels, polymorphic `as` prop, 5 slot types) and added it t
>
> **TO:** `eap-007` — *Known mistake entry*
> ## EAP-007: Adding Components to Main Site Without Playground Preview **Status: ACTIVE** **Trigger:** Creating a new reusable component in `src/components/` and integrating it into pages, but not creating a corresponding preview page in the playground (`playground/src/app/compon
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E057

**The case study review process doc mentions content feedback #19 briefly in parentheses — a terse inline citation.**

> **FROM:** `content-case-study-review` — *Sub-topic page*
> # Section 14 — Case Study Review Checklist > Standalone quality checklist. Usable by: > - `case-study-authoring` skill (Phase 4) > - `content-iteration` skill (when editing case study text) > - Rebuild audits (Phase 1b, pre-rebuild quality assessment) > - Future evaluator agent (as scoring rubric)
>
> **TO:** `cfb-019` — *Bug/issue report*
> #### CFB-019: "The information architecture here is just wrong — architecture first, rationale second" **Intent:** The content hierarchy within the Token Architecture interactive visual was inverted. It started with "Why semantic naming" (the rationale and comparison table) befo
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E058

**The Design hub's issue table mentions FB-105 (admin controls displacing content) in a summary row.**

> **FROM:** `design` — *Hub page (main topic overview)*
> # Design System — Accumulated Knowledge > **What this file is:** The hub file for the design knowledge base. Detailed section content lives in spoke files under `docs/design/`. This file contains the Section Index for routing, the Design Posture (read every time), Process Principles, and the Feedback Frequency Map. > > **Who reads this:** AI agents
>
> **TO:** `fb-105` — *Bug/issue report*
> #### FB-105: Block list content indented ~24px vs intro blurb in admin mode **Date:** 2026-04-04 **UX Intent:** All body content in a case study (intro blurb headline, scope statement, section headings, section bodies) should share the same left edge. In admin mode, the block lis
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E059

**The candidate anti-patterns list cites ENG-266 as evidence for a potential new anti-pattern about designing workflows without checking the data layer.**

> **FROM:** `candidate-anti-patterns` — *List of candidate patterns (not yet promoted)*
> # Candidate Anti-Patterns > **Scratch file.** Patterns listed here have NOT yet been promoted to the canonical AP/EAP/CAP catalogs. Each entry records a failure mode that surfaced during Plan B's feedback-log tagging pass (or Phase 0 sample) but did not match an existing anti-pattern with strong or approximate confidence. > > **Not for citation.** 
>
> **TO:** `eng-266` — *Bug/issue report*
> ### ENG-266: Baseline eval transcript capture — designed an Export-Transcript + copy-paste workflow without probing the JSONL on disk first **Date:** 2026-04-27 **Issue:** Plan C Phase 10 of the Knowledge Graph Initiative locked in `cursor-export-transcript` as the canonical evi
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

### E060

**The Engram case study lists FB-056 (interactive visual scoping) in a "Design:" reference list of relevant feedback.**

> **FROM:** `content-engram` — *Sub-topic page*
> # Engram (formerly Élan Design System) - Case Study Dossier > Bootstrapped 2026-04-03 by synthesizing CFB-013 through CFB-019 + design/engineering logs. > Full rebuild 2026-04-04: thesis pivoted from "AI-native design system" to "compounding design knowledge for vibe coding." > v3 rebuild 2026-04-06: thesis pivoted to "what happens when you stop de
>
> **TO:** `fb-056` — *Bug/issue report*
> #### FB-056: "Doing all this on the button — map to spacing tokens" **UX Intent:** Every component dimension must trace back to the design system's token grid. Hardcoded pixel values that sit between tokens create "token drift" — the design system says one thing, the component sa
>
> **How they're connected:** references (detected via citation:bare)

**Score (0–10):** ___

---

## Summary

After scoring, tally how many edges fall into each range:

| Score range | Category | Count |
|-------------|----------|-------|
| 0 | ambiguous | |
| 1–3 | noise | |
| 4–6 | relevant | |
| 7–10 | authoritative | |
| | **Total** | **60** |

> If ambiguous (score = 0) exceeds 12 entries (20%), the rubric needs revision before proceeding to C4.

---

> **Changelog**
>
> - **v2** (2026-05-03): Added context snippets for every FROM and TO node so the labeler can understand what each document is about without opening source files.
> - **v1** (2026-05-03): Plain-language explanations, 0–10 numeric scale.
> - **v0** (2026-05-03): Raw technical data (machine-readable).
