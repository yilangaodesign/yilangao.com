/**
 * Playground stub for the admin EditButton.
 *
 * This exists so that site components imported via @site/* (which reference
 * @/components/EditButton) resolve in the playground context. The playground
 * renders with isAdmin=false, so this is never visible.
 *
 * Source of truth: src/components/EditButton.tsx — keep signature in sync.
 */

export default function EditButton(_props: {
  collection: string;
  id: number | string;
  label?: string;
}) {
  return null;
}
